server:
  port: 8080
  servlet:
    context-path: /transportbooking


spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/transportbooking"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true